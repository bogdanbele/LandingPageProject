mixin card(cardTitle, imageSrc, buttonTitle, buttonClass)
    .card
        img.card-image(src=imageSrc)
        .card-bottom
            span.card-title= cardTitle 
            +button(buttonTitle,"#subscribe", buttonClass)